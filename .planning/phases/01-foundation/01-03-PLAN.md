---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/commands/init.ts
  - src/commands/doctor.ts
  - src/index.ts
  - src/utils/unity.ts
  - src/utils/platform.ts
  - src/utils/manifest.ts
  - src/utils/assets.ts
  - src/utils/mcp.ts
  - src/__tests__/utils/mcp.test.ts
  - src/__tests__/utils/manifest.test.ts
  - src/__tests__/commands/doctor.test.ts
  - src/__tests__/commands/init.test.ts
autonomous: true

must_haves:
  truths:
    - "No MCP code remains in the codebase (mcp.ts deleted, all MCP imports/references removed)"
    - "gamekit init installs the Unity Editor plugin from template/Editor/GameKit/ into Assets/Editor/GameKit/ instead of injecting MCP packages"
    - "gamekit init adds .gamekit/ to project .gitignore"
    - "gamekit init no longer generates .mcp.json or waits for MCP relay"
    - "gamekit doctor checks Unity plugin connection health instead of MCP config and relay"
    - "gamekit doctor uses the connection module to verify the HTTP bridge is working"
    - "All CLI commands support a --json global flag for explicit JSON output"
    - "Existing tests are updated to reflect MCP removal and new plugin-based flow"
  artifacts:
    - path: "src/commands/init.ts"
      provides: "Updated init command installing Unity plugin instead of MCP"
      contains: "Editor/GameKit"
    - path: "src/commands/doctor.ts"
      provides: "Updated doctor command checking plugin connection"
      contains: "healthCheck\\|getConnection\\|checkPluginConnection"
    - path: "src/index.ts"
      provides: "Updated CLI entry with --json global option"
      contains: "option.*--json"
  key_links:
    - from: "src/commands/doctor.ts"
      to: "src/utils/connection.ts"
      via: "Import and use healthCheck/readServerInfo for plugin connection check"
      pattern: "import.*connection"
    - from: "src/commands/init.ts"
      to: "template/Editor/GameKit/"
      via: "Copy Unity plugin files during init"
      pattern: "Editor.*GameKit"
    - from: "src/index.ts"
      to: "src/utils/output.ts"
      via: "Pass --json flag to commands"
      pattern: "json"
---

<objective>
Strip all MCP code from the codebase and update the init and doctor commands to use the new Unity plugin and connection layer.

Purpose: This is the "clean break" -- removing the third-party MCP relay dependency and wiring up the new HTTP bridge. After this plan, gamekit uses its own Unity plugin exclusively. init installs the plugin, doctor checks the plugin connection, and the CLI supports structured JSON output.

Output: Updated init.ts, doctor.ts, index.ts; cleaned-up utility modules; deleted MCP code; updated tests.
</objective>

<execution_context>
@/Users/teis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/teis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/commands/init.ts
@src/commands/doctor.ts
@src/index.ts
@src/utils/mcp.ts
@src/utils/manifest.ts
@src/utils/platform.ts
@src/utils/assets.ts
@src/utils/unity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip MCP code and clean up utility modules</name>
  <files>
    src/utils/mcp.ts
    src/utils/manifest.ts
    src/utils/platform.ts
    src/utils/unity.ts
    src/utils/assets.ts
    src/__tests__/utils/mcp.test.ts
    src/__tests__/utils/manifest.test.ts
  </files>
  <action>
Remove all MCP-related code from the codebase. This is a mechanical removal -- delete files and strip functions.

**DELETE entirely:**
- `src/utils/mcp.ts` -- entire file (MCP config generation, relay waiting, relay detection)
- `src/__tests__/utils/mcp.test.ts` -- entire test file

**MODIFY src/utils/platform.ts:**
- Remove the `getMcpRelayPath()` function entirely (lines 40-62)
- Keep `getPlatform()`, `isWindows()`, `isMac()`, `getHomeDir()` -- these are used by other modules
- Remove the `path` import if it becomes unused after removing `getMcpRelayPath`

**MODIFY src/utils/manifest.ts:**
- Remove the `addMcpToManifest()` function entirely
- Remove the import of `getMcpPackageUrl` from `./unity.js`
- The file can remain if there is other manifest functionality to add later (the module will be used in the future for other manifest operations). However, since it currently only contains `addMcpToManifest`, the file should be effectively emptied or deleted. Delete the file since it only contained MCP code.
- Also delete `src/__tests__/utils/manifest.test.ts` if it only tests MCP manifest functionality.

**MODIFY src/utils/unity.ts:**
- Remove the `getMcpPackageUrl()` function (lines 176-192)
- Keep everything else: `UnityInstall`, `UnityVersion`, `getUnityHubPath`, `parseUnityVersion`, `isUnity6OrNewer`, `getUnityExecutablePath`, `findUnityInstalls`, `createUnityProject`, `isUnityProject`, `openUnityProject`
- Update exports (the removed function was imported by init.ts and manifest.ts)

**MODIFY src/utils/assets.ts:**
- The current file creates a `ScreenshotCapture.cs` Editor script. This legacy screenshot utility will be superseded by the gamekit screenshot command in Phase 3.
- For now, keep the file but note that the inline C# string (`SCREENSHOT_CAPTURE_CS`) will be removed in a future phase. The `createEditorScripts()` function is still called by init.ts and still creates the screenshot utility. Leave it functional for now -- it does not conflict with the GameKit plugin.
- No changes needed to this file in this plan.

**Verification after this task:** grep the entire `src/` directory for "mcp" (case-insensitive) to confirm no MCP references remain except in git history.
  </action>
  <verify>
- Verify deleted: `! test -f src/utils/mcp.ts && ! test -f src/__tests__/utils/mcp.test.ts && ! test -f src/utils/manifest.ts && ! test -f src/__tests__/utils/manifest.test.ts`
- Verify platform.ts no longer has `getMcpRelayPath`
- Verify unity.ts no longer has `getMcpPackageUrl`
- Run: `grep -ri "mcp" src/ --include="*.ts" | grep -v "node_modules"` should return no results (or only comments explaining the removal)
  </verify>
  <done>
All MCP code removed: mcp.ts and its test deleted, manifest.ts and its test deleted, getMcpRelayPath removed from platform.ts, getMcpPackageUrl removed from unity.ts. No MCP references remain in src/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update init command to install Unity plugin, update doctor to check plugin connection, add --json flag</name>
  <files>
    src/commands/init.ts
    src/commands/doctor.ts
    src/index.ts
    src/__tests__/commands/doctor.test.ts
    src/__tests__/commands/init.test.ts
  </files>
  <action>
Update the three command files to use the new Unity plugin and connection layer instead of MCP.

**MODIFY src/index.ts:**
- Add a global `--json` option to the Commander program: `program.option('--json', 'Output raw JSON (default when stdout is piped)')`. This sets `options.json` on the parsed options object.
- Pass the `--json` option through to commands that need it. The simplest approach: commands can read it from `program.opts().json` when they need it.
- No other changes needed to index.ts.

**MODIFY src/commands/init.ts:**
- Remove all MCP imports: remove `addMcpToManifest` from manifest.js, remove `generateMcpConfig`, `waitForMcpRelay`, `mcpRelayExists` from mcp.js, remove `getMcpPackageUrl` from unity.js
- Add import for `copyGameKitPlugin` (new helper function, see below)

In `initExistingProject()`:
- Remove Step 2 entirely (addMcpToManifest -- lines 101-123)
- Remove Step 3 entirely (generateMcpConfig -- lines 125-136)
- Remove the `mcpRelayExists()` conditional at the end (lines 138-161)
- After the template copy step, add a new step: "Installing GameKit Unity plugin..."
  - Copy the `template/Editor/GameKit/` directory contents to `{projectPath}/Assets/Editor/GameKit/`
  - Create the `Assets/Editor/GameKit/` directory structure recursively
  - Copy all files maintaining the directory structure (Handlers/, Models/, Utils/ subdirectories)
  - Use a recursive copy utility (reuse the pattern from template.ts or write a simple recursive copy)
- Add a new step: "Configuring project..."
  - Read or create `.gitignore` at project root
  - Append `.gamekit/` to .gitignore if not already present (the port file directory should not be committed)
- Update the success message: remove all MCP references. Replace "Restart Unity to load the new MCP package" with "Open Unity to start the GameKit plugin" (for existing projects). Remove the "Wait for Unity to install packages" step.
- Remove the `unityVersion` prompt for existing projects -- it was only needed for `getMcpPackageUrl()`. The plugin does not need to know the Unity version at install time.

In `createNewProject()`:
- Remove Step 5 entirely (addMcpToManifest -- lines 258-280)
- Remove Step 6 entirely (generateMcpConfig -- lines 282-293)
- Remove Step 8 entirely (waitForMcpRelay -- lines 305-307)
- After the template copy step (Step 4), add the same Unity plugin copy step and .gitignore configuration as above
- Update success messages: remove MCP references, remove "Wait for Unity to install packages" step. New steps: "cd {projectName}", "Wait for Unity to finish loading", "claude"
- The `unityVersion` prompt is still needed for `createNewProject()` because it determines which Unity executable to use for project creation. But it is no longer used for MCP URL selection. Remove the `getMcpPackageUrl` call.

**Create a helper function** (either in init.ts or as a new small util):
- `copyGameKitPlugin(projectPath: string): void` -- copies `template/Editor/GameKit/` into `{projectPath}/Assets/Editor/GameKit/`
- Use `fs.cpSync(src, dest, { recursive: true })` (available in Node.js 16+ / Bun) or implement recursive copy
- The source path can be resolved relative to the CLI binary's location, similar to how `copyTemplateAsync` resolves the template path. Look at how `template.ts` locates the template directory and follow the same pattern.

**MODIFY src/commands/doctor.ts:**
- Remove imports from `../utils/mcp.js` (`hasMcpConfig`, `mcpRelayExists`)
- Add imports from `../utils/connection.js` (`readServerInfo`, `healthCheck`, `GameKitError`)
- Remove `checkMcpConfig()` function and `checkMcpRelay()` function
- Add `checkPluginInstalled()`: check if `Assets/Editor/GameKit/GameKitServer.cs` exists in current directory. Pass: "GameKit plugin installed". Fail fix: "Run: gamekit init"
- Add `async checkPluginConnection()`: try `readServerInfo(process.cwd())` then `healthCheck(info.port)`. If server info found and health check passes: "Unity plugin connected" with message showing port and Unity version. If port file not found: warn "Unity plugin not connected" with message "Open your project in Unity to start the plugin". If PID dead: warn with "Unity is not running". If health check fails: fail with "Plugin not responding, try restarting Unity".
- Update the `runDoctor()` function: replace `checkMcpConfig()` and `checkMcpRelay()` with `checkPluginInstalled()` and `await checkPluginConnection()`. Note: `runDoctor` is already async, so awaiting is fine.
- Keep `checkUnityInstalled()`, `checkUnityProject()`, `checkClaudeCommands()` as-is.

**UPDATE tests:**
- `src/__tests__/commands/doctor.test.ts`: update to test the new checks (checkPluginInstalled, checkPluginConnection) instead of MCP checks. Mock `readServerInfo` and `healthCheck` from connection module.
- `src/__tests__/commands/init.test.ts`: update to test that init copies plugin files instead of MCP config. Remove tests for MCP manifest injection and .mcp.json generation. Add tests for .gitignore update and plugin file copying.
  </action>
  <verify>
- `npm run typecheck` passes (no type errors)
- `npm test` passes (all tests pass)
- Verify init.ts has no MCP imports: `grep -c "mcp\|MCP\|mcpRelay\|\.mcp\.json\|addMcpToManifest\|getMcpPackageUrl" src/commands/init.ts` returns 0
- Verify doctor.ts has no MCP imports: `grep -c "mcp\|MCP\|mcpRelay\|\.mcp\.json" src/commands/doctor.ts` returns 0
- Verify doctor.ts imports from connection.ts: `grep "connection" src/commands/doctor.ts`
- Verify init.ts copies plugin files: `grep "Editor/GameKit" src/commands/init.ts`
- Verify index.ts has --json option: `grep "json" src/index.ts`
  </verify>
  <done>
Init command installs Unity Editor plugin (copies template/Editor/GameKit/ to Assets/Editor/GameKit/) and adds .gamekit/ to .gitignore instead of injecting MCP packages or generating .mcp.json. Doctor command checks for plugin installation and connection health via the connection module instead of MCP config/relay checks. CLI supports --json global flag. All MCP references removed from command files. Tests updated for new behavior.
  </done>
</task>

</tasks>

<verification>
Full-codebase MCP removal verification:
1. `grep -ri "mcp" src/ --include="*.ts"` returns zero results
2. `src/utils/mcp.ts` does not exist
3. `src/utils/manifest.ts` does not exist
4. `npm run typecheck` passes
5. `npm test` passes
6. Init command references `Editor/GameKit` (plugin copy)
7. Doctor command references `connection.ts` (health check)
8. Index.ts has `--json` option
</verification>

<success_criteria>
- [ ] Zero MCP references remain in src/ (no mcp.ts, no manifest.ts, no getMcpRelayPath, no getMcpPackageUrl, no .mcp.json generation, no relay waiting)
- [ ] `gamekit init` copies template/Editor/GameKit/ into Assets/Editor/GameKit/
- [ ] `gamekit init` adds .gamekit/ to project .gitignore
- [ ] `gamekit init` no longer prompts for Unity version in existing project flow (was only needed for MCP URL)
- [ ] `gamekit doctor` checks for plugin files in Assets/Editor/GameKit/
- [ ] `gamekit doctor` checks plugin connection via readServerInfo + healthCheck
- [ ] CLI supports --json global flag
- [ ] Type checking passes
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
